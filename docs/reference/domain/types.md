<!-- GENERATED by scripts/gen-reference.mjs — do not edit by hand.
     A gate compares this file byte for byte with what the generator produces now, so an edit
     here is a failure, not a change. Change `scripts/reference-prose.mjs` and regenerate. -->

# `domain/types`

Bars, points, scopes, panes, series and the two branded identifiers. Nothing here imports
React or a canvas, so importing a type from this module costs nothing at runtime. `paneId` and
`seriesId` are the only way to make an identifier, which keeps a bare string from drifting into the
wrong slot; `utcSeconds` does the same for time.

## Example

```ts
import { paneId, seriesId, utcSeconds, type Bar, type PaneSpec } from 'lightweight-magic-charts';

const bar: Bar = { time: utcSeconds(1_700_000_000), open: 100, high: 102, low: 99, close: 101 };

export const pane: PaneSpec = {
  id: paneId('price'),
  title: 'Price action',
  format: { kind: 'price', minMove: 0.01 },
  series: [{ id: seriesId('close'), label: 'Close', shape: 'line', color: '#4c8dff' }],
  defaultVisible: true,
};
export const firstClose: number = bar.close;
```

## Exports

- **`auditDirectionEncoding`** · value · `(panes: readonly PaneSpec[]) => readonly DirectionEncodingViolation[]`
- **`Bar`** · type · `interface Bar { time: UtcSeconds; open: number; high: number; low: number; close: number; volume?: number | undefined }`
- **`Direction`** · type · `type Direction = 'up' | 'down' | 'flat'`
- **`DirectionChannel`** · type · `type DirectionChannel = 'color' | 'position' | 'shape'`
- **`directionConvention`** · value · `(input: { readonly upColor: string; readonly downColor: string; readonly encodeDirectionBy?: readonly DirectionChannel[]; }) => PriceScaleConvention`
- **`DirectionEncoding`** · type · `interface DirectionEncoding { direction: Direction; color: string | null; side: 0 | 1 | -1; glyph: DirectionGlyph }`
- **`DirectionEncodingViolation`** · type · `interface DirectionEncodingViolation { pane: PaneId; series: SeriesId; reason: string }`
- **`DirectionGlyph`** · type · `type DirectionGlyph = 'apex-up' | 'apex-down' | 'none'`
- **`directionOf`** · value · `(value: number, reference?: number) => Direction`
- **`encodeDirection`** · value · `(convention: PriceScaleConvention, value: number, reference?: number) => DirectionEncoding`
- **`invertConvention`** · value · `(convention: PriceScaleConvention) => PriceScaleConvention`
- **`isGap`** · value · `(p: Point) => boolean`
- **`nonChromaticChannels`** · value · `(convention: PriceScaleConvention) => readonly DirectionChannel[]`
- **`paneId`** · value · `(s: string) => PaneId`
- **`PaneId`** · type · `type PaneId = string & { readonly __brand: 'PaneId' }`
- **`PaneSpec`** · type · `interface PaneSpec { id: PaneId; title: string; format: ValueFormat; series: readonly SeriesSpec[]; targetHeightPx?: number | undefined; referenceLine?: number | undefined; defaultVisible: boolean }`
- **`PaneState`** · type · `type PaneState = | { readonly kind: 'visible'; readonly heightPx: number } | { readonly kind: 'collapsed' }`
- **`Point`** · type · `type Point = | { readonly time: UtcSeconds; readonly value: number; readonly color?: string } | { readonly time: UtcSeconds }`
- **`PortResult`** · type · `type PortResult<T> = | { readonly ok: true; readonly value: T } | { readonly ok: false; readonly reason: 'aborted' | 'transport' | 'contract'; readonly detail: string; }`
- **`PriceScaleConvention`** · type · `interface PriceScaleConvention { upColor: string; downColor: string; encodeDirectionBy: readonly DirectionChannel[] }`
- **`sameScope`** · value · `(a: Scope, b: Scope) => boolean`
- **`Scope`** · type · `interface Scope { instrument: string; resolution: string; venue?: string | undefined; market?: string | undefined }`
- **`scopeKey`** · value · `(scope: Scope) => string`
- **`seriesId`** · value · `(s: string) => SeriesId`
- **`SeriesId`** · type · `type SeriesId = string & { readonly __brand: 'SeriesId' }`
- **`SeriesSpec`** · type · `interface SeriesSpec { id: SeriesId; label: string; shape: 'line' | 'histogram' | 'area'; color: string; lineWidth?: 1 | 2 | undefined; signColoring?: boolean | undefined; barDirectionColoring?: boolean | undefined; format?: ValueFormat | undefined; ownScale?: boolean | undefined; mirrored?: boolean | undefined; stepCarry?: boolean | undefined }`
- **`utcSeconds`** · value · `(n: number) => UtcSeconds`
- **`UtcSeconds`** · type · `type UtcSeconds = number & { readonly __brand: 'UtcSeconds' }`
- **`ValueFormat`** · type · `type ValueFormat = | { readonly kind: 'price'; readonly minMove: number } | { readonly kind: 'percent'; readonly decimals: number } | { readonly kind: 'compact'; readonly decimals: number } | { readonly kind: 'ratio'; readonly decimals: number } | { readonly kind: 'custom'; readonly format: (v: number) => string; readonly minMove: number }`

Back to the [reference map](../_index.md) · why it is shaped this way is under
[`explanation/`](../../explanation/README.md).
