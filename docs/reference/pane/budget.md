<!-- GENERATED by scripts/gen-reference.mjs — do not edit by hand.
     A gate compares this file byte for byte with what the generator produces now, so an edit
     here is a failure, not a change. Change `scripts/reference-prose.mjs` and regenerate. -->

# `pane/budget`

What a previous visit saved is not authority: a pane that left the catalogue must go, and
one that arrived must appear. `reconcilePaneLayout` performs that merge on untrusted input and
always returns a usable layout.

## Example

```ts
import { reconcilePaneLayout, type PaneLayout } from 'lightweight-magic-charts';

declare const stored: unknown;

export const layout: PaneLayout = reconcilePaneLayout(
  stored,
  [{ id: 'price', defaultVisible: true, heightPx: 320 }],
  ['1h', '4h'],
  2,
);
```

## Exports

- **`CataloguedPane`** · type · `interface CataloguedPane { spec: { readonly id: string | number; readonly defaultVisible: boolean; readonly targetHeightPx?: number; readonly title?: string; } }`
- **`DEFAULT_INDICATOR_FLOOR_PX`** · value · `56`
- **`PaneCatalogueEntry`** · type · `interface PaneCatalogueEntry { id: string; defaultVisible: boolean; heightPx: number; title?: string | undefined }`
- **`PaneConfig`** · type · `interface PaneConfig { id: string; visible: boolean; heightPx: number; title?: string | undefined }`
- **`PaneLayout`** · type · `interface PaneLayout { version: number; panes: readonly PaneConfig[]; timeframe: string | null }`
- **`reconcilePaneLayout`** · value · `(raw: unknown, catalogue: readonly PaneCatalogueEntry[], servedTimeframes: readonly string[], version: number) => PaneLayout`
- **`toCatalogueEntry`** · value · `(pane: CataloguedPane) => PaneCatalogueEntry`

Back to the [reference map](../_index.md) · why it is shaped this way is under
[`explanation/`](../../explanation/README.md).
