<!-- GENERATED by scripts/gen-reference.mjs — do not edit by hand.
     A gate compares this file byte for byte with what the generator produces now, so an edit
     here is a failure, not a change. Change `scripts/reference-prose.mjs` and regenerate. -->

# `react/chrome/labels`

English is the default, not the only option: each member is overridable, at either level,
and text that depends on a value is a function so a host keeps control of word order.
`resolveWorkspaceLabels` merges an override over the default member by member, never by spread.

## Example

```ts
import { resolveWorkspaceLabels, type WorkspaceChromeLabels } from 'lightweight-magic-charts';

export const labels: WorkspaceChromeLabels = resolveWorkspaceLabels({
  dismiss: 'Fechar',
  sections: { panes: 'Painéis' },
});
```

## Exports

- **`DEFAULT_WORKSPACE_CHROME_LABELS`** · value · `WorkspaceChromeLabels`
- **`GridControlsLabels`** · type · `interface GridControlsLabels { mode: string; addCell: string }`
- **`OverlayTogglesLabels`** · type · `interface OverlayTogglesLabels { density: string; profile: string }`
- **`PaneListLabels`** · type · `interface PaneListLabels { group: string; show: (pane: string) => string; up: (pane: string) => string; down: (pane: string) => string; row: (pane: string, at: number, of: number) => string; handle: string }`
- **`PatternChipsLabels`** · type · `interface PatternChipsLabels { group: string }`
- **`PrimaryActionsLabels`** · type · `interface PrimaryActionsLabels { autoFit: string; addPriceLine: string }`
- **`resolveWorkspaceLabels`** · value · `(overrides?: WorkspaceLabelOverrides, locale?: string) => WorkspaceChromeLabels`
- **`StatusFooterLabels`** · type · `interface StatusFooterLabels { loading: string; shrunk: (percent: number) => string; evicted: (panes: readonly string[]) => string; alerts: (names: readonly string[]) => string }`
- **`StudiesPanelLabels`** · type · `interface StudiesPanelLabels { trigger: string; panel: (chosen: number, capacity: number) => string; none: string; remove: (name: string) => string; up: (name: string) => string; down: (name: string) => string; noData: string; warmUp: (warmUpBars: number, windowBars: number) => string; truncated: (drawn: number, total: number) => string }`
- **`StylePickerLabels`** · type · `interface StylePickerLabels { trigger: string; group: (series: string) => string; shape: (shape: string, series: string) => string }`
- **`SymbolTriggerLabels`** · type · `interface SymbolTriggerLabels { trigger: (symbol: string) => string; empty: string }`
- **`WorkspaceChromeLabels`** · type · `interface WorkspaceChromeLabels { dismiss: string; workspace: (symbol: string) => string; canvas: (symbol: string) => string; state: (symbol: string, timeframe: string, panes: number) => string; tabName: (index: number) => string; laneTitle: string; interval: string; priceAlert: (alert: PriceAlert) => string; sections: WorkspaceSectionLabels; notices: WorkspaceNoticeLabels; panes: PaneListLabels; overlays: OverlayTogglesLabels; patterns: PatternChipsLabels; grid: GridControlsLabels; primary: PrimaryActionsLabels; symbol: SymbolTriggerLabels; status: StatusFooterLabels; style: StylePickerLabels; studies: StudiesPanelLabels; seriesMenu: SeriesMenuLabels; tabsBar: WorkspaceTabsBarLabels; drawingToolbar: DrawingToolbarLabels; compactCell: CompactCellLabels; density: DensityControlLabels }`
- **`WorkspaceLabelOverrides`** · type · `type WorkspaceLabelOverrides = { readonly [K in keyof WorkspaceChromeLabels]?: WorkspaceChromeLabels[K] extends ( ...args: never[] ) => unknown ? WorkspaceChromeLabels[K] : WorkspaceChromeLabels[K] extends object ? Partial<WorkspaceChromeLabels[K]> : WorkspaceChromeLabels[K]; }`
- **`WorkspaceNoticeLabels`** · type · `interface WorkspaceNoticeLabels { noBars: (symbol: string) => string; unverifiedSeam: (symbol: string) => string; degenerate: (px: number) => string; studyLimit: (capacity: number) => string; tabLimit: (capacity: number) => string; unreadableTabs: string }`
- **`WorkspaceSectionLabels`** · type · `interface WorkspaceSectionLabels { panes: string; overlays: string; patterns: string }`

Back to the [reference map](../../_index.md) · why it is shaped this way is under
[`explanation/`](../../../explanation/README.md).
