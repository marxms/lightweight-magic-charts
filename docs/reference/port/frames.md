<!-- GENERATED by scripts/gen-reference.mjs — do not edit by hand.
     A gate compares this file byte for byte with what the generator produces now, so an edit
     here is a failure, not a change. Change `scripts/reference-prose.mjs` and regenerate. -->

# `port/frames`

Snapshot, append, amend, open, member — the frame vocabulary, plus the envelope that
carries a batch and the tip that holds the newest values. `resolveChannelShape` settles what a
channel really is when the declaration and the behaviour disagree.

## Example

```ts
import { resolveChannelShape, type ChannelShape } from 'lightweight-magic-charts';

export const shape: ChannelShape = resolveChannelShape('delta', true);
```

## Exports

- **`applyLiveEnvelope`** · value · `<TPayload>(state: LiveTip, envelope: LiveEnvelope<TPayload>, scope: Scope, project: (payload: TPayload) => ReadonlyMap<SeriesId, number>) => LiveTip`
- **`ChannelDescriptor`** · type · `interface ChannelDescriptor { shape: ChannelShape; scope: Scope; seriesIds: readonly SeriesId[] }`
- **`ChannelShape`** · type · `type ChannelShape = 'delta' | 'snapshot' | 'seeded-delta'`
- **`EMPTY_LIVE_TIP`** · value · `LiveTip`
- **`Frame`** · type · `type Frame = | { readonly kind: 'snapshot'; readonly gen: number; readonly seq: number; readonly scope: Scope; readonly state: ReadonlyMap<SeriesId, number>; readonly baseline?: number; readonly baselineTime?: number; } | { readonly kind: 'append'; readonly gen: number; readonly seq: number; readonly scope: Scope; readonly bar: Bar; readonly points?: ReadonlyMap<SeriesId, number>; } | { readonly kind: 'amend'; readonly gen: number; readonly seq: number; readonly scope: Scope; readonly bar: Bar; } | { readonly kind: 'open'; readonly gen: number; readonly scope: Scope; readonly bar: Bar; readonly points?: ReadonlyMap<SeriesId, number>; } | { readonly kind: 'member'; readonly gen: number; readonly seq: number; readonly scope: Scope; readonly op: 'upsert' | 'remove'; readonly key: string; } | { readonly kind: 'reset'; readonly gen: number; readonly scope: Scope; readonly cause: ResetCause; }`
- **`LinkStatus`** · type · `type LinkStatus = 'connecting' | 'open' | 'reconnecting' | 'closed'`
- **`LiveEnvelope`** · type · `interface LiveEnvelope { gen: number; seq: number; scope: Scope; payload: TPayload }`
- **`LiveTip`** · type · `interface LiveTip { gen: number | null; seq: number | null; values: ReadonlyMap<SeriesId, number> }`
- **`ResetCause`** · type · `type ResetCause = 'gap' | 'amend' | 'generation' | 'reconnect' | 'server' | 'stale-history'`
- **`resolveChannelShape`** · value · `(declared: ChannelShape, accumulatePerBar: boolean) => ChannelShape`

Back to the [reference map](../_index.md) · why it is shaped this way is under
[`explanation/`](../../explanation/README.md).
