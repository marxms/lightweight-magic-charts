<!-- GENERATED by scripts/gen-reference.mjs — do not edit by hand.
     A gate compares this file byte for byte with what the generator produces now, so an edit
     here is a failure, not a change. Change `scripts/reference-prose.mjs` and regenerate. -->

# `port/chartApi`

Every member the workspace needs from a chart, declared structurally so no runtime import
of `lightweight-charts` exists anywhere in `src`. A host implements `ChartEngine` once; the
package never sees the real package. That constraint is why the CommonJS build still works.

## Example

```ts
import type { ChartEngine, WorkspaceChartHandle } from 'lightweight-magic-charts';

declare const makeHandle: (container: HTMLElement) => WorkspaceChartHandle;

export const engine: ChartEngine = (container, _options) => makeHandle(container);
```

## Exports

- **`BitmapScope`** · type · `interface BitmapScope { context: CanvasRenderingContext2D; mediaSize: { readonly width: number; readonly height: number; }; horizontalPixelRatio: number; verticalPixelRatio: number }`
- **`BitmapTarget`** · type · `interface BitmapTarget { useBitmapCoordinateSpace: (fn: (scope: BitmapScope) => void) => void }`
- **`ChartClickParam`** · type · `interface ChartClickParam { point?: { readonly x: number; readonly y: number; } | undefined; paneIndex?: number | undefined; time?: unknown }`
- **`ChartEngine`** · type · `type ChartEngine = ( container: HTMLElement, options: Record<string, unknown>, ) => WorkspaceChartHandle`
- **`ChartLifecycle`** · type · `interface ChartLifecycle { applyOptions: (options: Record<string, unknown>) => void; subscribeCrosshairMove: (handler: (param: CrosshairParam) => void) => void; unsubscribeCrosshairMove: (handler: (param: CrosshairParam) => void) => void; remove: () => void; panes: () => readonly PaneHandle[]; addPane: (preserveEmptyPane?: boolean) => PaneHandle }`
- **`CrosshairParam`** · type · `interface CrosshairParam { point?: { readonly x: number; readonly y: number; } | undefined; paneIndex?: number | undefined; time?: unknown }`
- **`HorzScaleItem`** · type · `type HorzScaleItem = | number | string | { readonly year: number; readonly month: number; readonly day: number }`
- **`PaneChartHandle`** · type · `interface PaneChartHandle { panes: () => readonly PaneHandle[]; addPane: (preserveEmptyPane?: boolean) => PaneHandle }`
- **`PaneHandle`** · type · `interface PaneHandle { paneIndex: () => number; getStretchFactor: () => number; setStretchFactor: (stretchFactor: number) => void; setPreserveEmptyPane: (preserve: boolean) => void; moveTo: (paneIndex: number) => void; getHTMLElement: () => HTMLElement | null }`
- **`PriceConverter`** · type · `interface PriceConverter { priceToCoordinate: (price: number) => number | null }`
- **`PriceLineHandle`** · type · `interface PriceLineHandle { applyOptions: (options: Partial<PriceLineOptions>) => void }`
- **`PriceLineOptions`** · type · `interface PriceLineOptions { price: number; color?: string | undefined; lineWidth?: 1 | 2 | 3 | 4 | undefined; lineStyle?: number | undefined; axisLabelVisible?: boolean | undefined; title?: string | undefined; lineVisible?: boolean | undefined }`
- **`PriceScaleHandle`** · type · `interface PriceScaleHandle { applyOptions: (options: { readonly scaleMargins?: { readonly top: number; readonly bottom: number; }; readonly autoScale?: boolean; }) => void }`
- **`PrimitiveHost`** · type · `interface PrimitiveHost { attachPrimitive: (primitive: TPrimitive) => void; detachPrimitive: (primitive: TPrimitive) => void }`
- **`ScaleChartHandle`** · type · `interface ScaleChartHandle { timeScale: () => TimeScaleHandle }`
- **`SeriesHandle`** · type · `interface SeriesHandle { coordinateToPrice: (coordinate: number) => number | null; setData: (data: readonly unknown[]) => void; applyOptions: (options: Record<string, unknown>) => void; priceScale: () => PriceScaleHandle; createPriceLine: (options: PriceLineOptions) => PriceLineHandle; removePriceLine: (line: PriceLineHandle) => void; setMarkers?: ((markers: readonly SeriesMarkerPoint[]) => void) | undefined; priceToCoordinate: (price: number) => number | null; attachPrimitive: (primitive: unknown) => void; detachPrimitive: (primitive: unknown) => void }`
- **`SeriesMarkerPoint`** · type · `interface SeriesMarkerPoint { time: number; position: 'aboveBar' | 'belowBar' | 'inBar'; shape: 'circle' | 'square' | 'arrowUp' | 'arrowDown'; color: string; text?: string | undefined }`
- **`SeriesShape`** · type · `type SeriesShape = 'candlestick' | 'line' | 'histogram' | 'area'`
- **`TimeScaleHandle`** · type · `interface TimeScaleHandle { timeToCoordinate: (time: HorzScaleItem) => number | null; options: () => { readonly barSpacing: number; }; width: () => number }`
- **`WorkspaceChartHandle`** · type · `interface WorkspaceChartHandle { addSeries: (shape: SeriesShape, options: Record<string, unknown>, paneIndex?: number) => SeriesHandle; timeScale: () => { fitContent(): void; coordinateToTime?(coordinate: number): unknown; }; subscribeClick?: ((handler: (param: ChartClickParam) => void) => void) | undefined; unsubscribeClick?: ((handler: (param: ChartClickParam) => void) => void) | undefined; applyOptions: (options: Record<string, unknown>) => void; subscribeCrosshairMove: (handler: (param: CrosshairParam) => void) => void; unsubscribeCrosshairMove: (handler: (param: CrosshairParam) => void) => void; remove: () => void; panes: () => readonly PaneHandle[]; addPane: (preserveEmptyPane?: boolean) => PaneHandle }`

Back to the [reference map](../_index.md) · why it is shaped this way is under
[`explanation/`](../../explanation/README.md).
