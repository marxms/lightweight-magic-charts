<!-- GENERATED by scripts/gen-reference.mjs — do not edit by hand.
     A gate compares this file byte for byte with what the generator produces now, so an edit
     here is a failure, not a change. Change `scripts/reference-prose.mjs` and regenerate. -->

# `domain/readings`

A series with holes draws a broken line unless something decides what a hole means.
`carryReadings` applies the spec's own answer — `stepCarry` holds the last value, everything else
leaves the gap — so the decision lives with the series rather than with each renderer.

## Example

```ts
import { carryReadings, type SeriesSpec } from 'lightweight-magic-charts';

declare const spec: SeriesSpec;

export const carried: readonly (number | null)[] = carryReadings([1, null, null, 4], spec);
```

## Exports

- **`carryReadings`** · value · `(raw: readonly (number | null)[], spec: SeriesSpec) => Array<number | null>`

Back to the [reference map](../_index.md) · why it is shaped this way is under
[`explanation/`](../../explanation/README.md).
